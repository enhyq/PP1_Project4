package com.enhyq.pp1.project4.util;

public class BoardFileUpload {

//    String photo = "";
//    int sizeLimit = 15*1024*1024;
//    String realPath = request.getServletContext().getRealPath("upload");
//
//    BoardV0 one = null;
//    MultipartRequest multipartRequest = null;
//    try {
//        multipartRequest = new MultipartRequest(request, realPath, sizeLimit, "utf-8", new DefaultFileRenamePolicy());
//        one = new BoardV0();
//        photo = multipartRequest.getFilesystemName("photo");
//        String seq = multipartRequest.getParameter("seq");
//        if(seq != null && !seq.equals("")){
//            int id = Integer.parseInt(seq);
//            one.setSeq(id);
//            String oldphoto = new BoardDAO().getPhotoName(id);
//            if(photo != null) deleteOldPhoto(realPath, oldphoto);
//            else photo = oldphoto;
//        }
//        one.setTitle(multipartRequest.getParameter("title"));
//        one.setWriter(multipartRequest.getParameter("writer"));
//
//    }

}
